mongodb:
  enabled: true
  replicaCount: 1
  image:
    tag: "7.0"
  auth:
    enabled: true
    rootUsername: admin
    rootPassword: "stackai-dev-password"
    rootDatabase: admin
  persistence:
    enabled: true
    size: 5Gi
  resources:
    requests:
      memory: 512Mi
      cpu: 200m
    limits:
      memory: 1Gi
      cpu: 1000m
